name: MLProject

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: spam_email_dataset_cleaned.csv}
    command: "python modelling.py --data_path {data_path}"
